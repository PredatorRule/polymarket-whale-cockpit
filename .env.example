# Vite public env (safe to expose — anon key is designed for the browser).
# Copy to .env and fill in the real values, then restart `vite`.
VITE_SUPABASE_URL="https://sffvdwggseglagogehzk.supabase.co"
VITE_SUPABASE_ANON_KEY="PASTE_YOUR_ANON_PUBLIC_KEY_HERE"
